<resources>
    <style name="Theme.Door2DoorDonations.Splash" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/app_background_gradient</item>
        <!-- On API 31+, setting windowIsTranslucent to true usually disables the system splash screen -->
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
    </style>
</resources>
